{
    "buildCommand": "cd frontend && npm i && npm run build",
    "outputDirectory": "client/dist",
    "framework": "vite",
    "rewrites": {
        "source": "api/(.*)",
        "destination": "/api"
    }
}